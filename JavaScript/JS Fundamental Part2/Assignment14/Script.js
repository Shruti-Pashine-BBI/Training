const ages = 18;
ages >= 18 ? "I want to drink wine" : "I want to drink water";

const newvalue = ages >= 18 ? "wine" : "water";
console.log(newvalue);

const age = prompt("Please Enter Your Age");
const res = age >= 18 ? "Adult" : "kid";
document.write("You are" + " " + res + " " + "Please Continue");
